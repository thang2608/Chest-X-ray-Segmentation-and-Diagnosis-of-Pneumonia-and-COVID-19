# requirements-model.txt — Model Lead packages (tách khỏi requirements chung của backend)
#
# QUAN TRỌNG: torch và torchvision phải cài TRƯỚC với CUDA flag, KHÔNG dùng file này để cài chúng.
# Lý do: pip install torch từ PyPI mặc định sẽ lấy bản CPU, ghi đè mất bản CUDA đang có.
#
# Cài torch + torchvision với CUDA :
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Sau đó mới chạy:
#   pip install -r requirements-model.txt

albumentations>=1.3.1
segmentation-models-pytorch>=0.3.3
grad-cam>=1.5.0
scikit-learn>=1.3.0
matplotlib>=3.8.0
seaborn>=0.13.0
torchinfo>=1.8.0
tqdm>=4.66.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0